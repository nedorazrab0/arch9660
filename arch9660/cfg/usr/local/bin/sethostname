#!/usr/bin/env bash
#
# Set random host name
set -e

[[ -d '/run/archiso' ]] && suff='-live'
hostname="$(python3 '/usr/local/bin/hostnamegen')"
 "${hostname}${suff}"

echo "${hostname}${suff}" > '/etc/hostname'
chmod 444 '/etc/hostname'
chattr +i '/etc/hostname'
